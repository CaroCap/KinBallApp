{% extends 'base.html.twig' %}

{% block title %}SeanceEntrainement{% endblock %}

{% block body %}
    <h1>SeanceEntrainement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ seance_entrainement.id }}</td>
            </tr>
            <tr>
                <th>Start</th>
                <td>{{ seance_entrainement.start ? seance_entrainement.start|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ seance_entrainement.title }}</td>
            </tr>
            <tr>
                <th>HeureDebut</th>
                <td>{{ seance_entrainement.heureDebut ? seance_entrainement.heureDebut|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>HeureFin</th>
                <td>{{ seance_entrainement.heureFin ? seance_entrainement.heureFin|date('H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_entrainement_index') }}">back to list</a>

    <a href="{{ path('app_entrainement_edit', {'id': seance_entrainement.id}) }}">edit</a>

    {{ include('entrainement/_delete_form.html.twig') }}
{% endblock %}
