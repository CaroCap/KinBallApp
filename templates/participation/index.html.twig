{% extends 'base.html.twig' %}

{% block title %}ParticipationEntrainement index{% endblock %}

{% block body %}
    <h1>ParticipationEntrainement index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>TypePresence</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for participation_entrainement in participation_entrainements %}
            <tr>
                <td>{{ participation_entrainement.id }}</td>
                <td>{{ participation_entrainement.typePresence }}</td>
                <td>
                    <a href="{{ path('app_participation_show', {'id': participation_entrainement.id}) }}">show</a>
                    <a href="{{ path('app_participation_edit', {'id': participation_entrainement.id}) }}">edit</a>

                    {# <div><label for="inscription_jourEntrainement" class="required">Jour entrainement</label><select id="inscription_jourEntrainement" name="inscription[jourEntrainement]"><option value="Mercredi">Mercredi</option><option value="Dimanche">Dimanche</option><option value="Mercredi &amp; Dimanche">Mercredi &amp; Dimanche</option></select></div> #}

                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
