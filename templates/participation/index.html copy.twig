{% extends 'base.html.twig' %}

{% block title %}Participation index{% endblock %}

{% block body %}
    <h1>Participation index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>TypePresence</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for participation_entrainement in participation_entrainements %}
            <tr>
                <td>{{ participation_entrainement.id }}</td>
                <td>{{ participation_entrainement.typePresence }}</td>
                <td>
                    <a href="{{ path('app_participation_show', {'id': participation_entrainement.id}) }}">show</a>
                    <a href="{{ path('app_participation_edit', {'id': participation_entrainement.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_participation_new') }}">Create new</a>
{% endblock %}
