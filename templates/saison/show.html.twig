{% extends 'base.html.twig' %}

{% block title %}Saison{% endblock %}

{% block body %}
    <h1>Saison</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ saison.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ saison.titre }}</td>
            </tr>
            <tr>
                <th>Debut</th>
                <td>{{ saison.debut ? saison.debut|date('d-m-Y') : '' }}</td>
            </tr>
            <tr>
                <th>Fin</th>
                <td>{{ saison.fin ? saison.fin|date('d-m-Y') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_saison_index') }}">back to list</a>

    <a href="{{ path('app_saison_edit', {'id': saison.id}) }}">edit</a>
    
    {# ! Ajouter des séances #}
    {# <a href="{{ path('app_seance_new', {'id': saison.id}) }}">New Séances</a> #}

{# Delete ne fonctionne pas #}
    {# {{ include('saison/_delete_form.html.twig') }} #}
{% endblock %}
