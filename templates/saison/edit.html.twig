{% extends 'base.html.twig' %}

{% block title %}Edit Saison{% endblock %}

{% block body %}
    <h1>Edit Saison</h1>

    {{ include('saison/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_saison_index') }}">back to list</a>

    {# ! ajout séance en lien avec la saison ? #}
    {# <a href="{{ path('') }}">Ajouter des séances</a> #}

{# Delete ne fonctionne pas -> cascade pour éviter orphelin ? #}
    {# {{ include('saison/_delete_form.html.twig') }} #}
{% endblock %}
