{% extends 'base.html.twig' %}

{% block title %}New Seance{% endblock %}

{% block filtre %}
    {{ include('seance/_baseNewSeance.html.twig') }}
{% endblock %}

{% block body %}
    <h1>Create new Seance</h1>

    {{ include('seance/_form.html.twig') }}

    {# <a href="{{ path('app_seance_index') }}">back to list</a> #}
    <a href="{{ path('app_seance_index') }}">back to list</a>
{% endblock %}
